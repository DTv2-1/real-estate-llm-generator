{
  "title": "Real Estate Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "start_urls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of URLs to scrape property listings from",
      "editor": "requestListSources",
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "title": "URL",
            "pattern": "^https?://.+"
          }
        },
        "required": ["url"]
      },
      "prefill": [
        {
          "url": "https://www.coldwellbankercostarica.com/property/search"
        },
        {
          "url": "https://www.encuentra24.com/costa-rica-en/real-estate-for-sale"
        }
      ]
    },
    "use_residential_proxies": {
      "title": "Use Residential Proxies",
      "type": "boolean",
      "description": "Enable residential proxies for Cloudflare bypass. Required for encuentra24.com",
      "default": true,
      "editor": "checkbox"
    },
    "proxy_country": {
      "title": "Proxy Country",
      "type": "string",
      "description": "Two-letter country code for proxy geolocation",
      "default": "CR",
      "enum": ["CR", "US", "MX", "PA"],
      "enumTitles": ["Costa Rica", "United States", "Mexico", "Panama"]
    },
    "max_listings": {
      "title": "Max Listings",
      "type": "integer",
      "description": "Maximum number of listings to scrape. 0 for unlimited",
      "default": 100,
      "minimum": 0
    }
  },
  "required": ["start_urls"]
}
